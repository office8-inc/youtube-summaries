name: Auto Merge Copilot PR

on:
  # 5分おきに定期実行
  schedule:
    - cron: '*/5 * * * *'
  # 手動実行も可能
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    
    steps:
      - name: Find and process Copilot PR
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          echo "🔍 Copilot coding agent完了。関連PRを検索..."
          
          # Copilotブランチの未マージPRを検索（最新1件のみ）
          PR_INFO=$(gh pr list --repo ${{ github.repository }} --state open --json number,headRefName,isDraft \
            | jq -r '[.[] | select(.headRefName | startswith("copilot/"))] | .[0]')
          
          if [ "$PR_INFO" = "null" ] || [ -z "$PR_INFO" ]; then
            echo "⚠️ Copilot PRが見つかりません"
            exit 0
          fi
          
          PR_NUMBER=$(echo "$PR_INFO" | jq -r '.number')
          IS_DRAFT=$(echo "$PR_INFO" | jq -r '.isDraft')
          
          echo "PR #$PR_NUMBER を検出 (Draft: $IS_DRAFT)"
          
          # Draft PRの場合はReady化
          if [ "$IS_DRAFT" = "true" ]; then
            echo "📝 Draft PRをReady for Reviewに変更..."
            gh pr ready $PR_NUMBER --repo ${{ github.repository }}
            echo "✓ Ready状態に変更しました"
            sleep 5
          fi
          
          # PRを自動承認
          echo "✅ PRを自動承認します: #$PR_NUMBER"
          gh pr review $PR_NUMBER \
            --repo ${{ github.repository }} \
            --approve \
            --body "✅ Copilotによる改善を自動承認しました。" || echo "⚠️ 承認をスキップ"
          
          # PRを自動マージ
          echo "🔀 PRを自動マージします: #$PR_NUMBER"
          if gh pr merge $PR_NUMBER \
            --repo ${{ github.repository }} \
            --squash \
            --delete-branch; then
            echo "✅ 自動マージが完了しました"
          else
            echo "⚠️ 直接マージ失敗、auto-mergeを試行..."
            gh pr merge $PR_NUMBER \
              --repo ${{ github.repository }} \
              --auto \
              --squash \
              --delete-branch && echo "✅ Auto-mergeを設定しました"
          fi

