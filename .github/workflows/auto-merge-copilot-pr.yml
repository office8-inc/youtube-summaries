name: Auto Merge Copilot PR

on:
  pull_request_target:
    types: [ready_for_review]  # Draft解除時のみ実行（重複実行を防止）
    paths:
      - 'xserver/summaries/**/*.md'

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    # CopilotブランチからのPRのみ自動マージ（より確実な判定）
    if: contains(github.event.pull_request.head.ref, 'copilot/')
    runs-on: ubuntu-latest
    
    steps:
      - name: Wait for Copilot to finish
        run: sleep 10
      
      - name: Check and fix Draft PR
        id: pr-check
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN || github.token }}
        run: |
          PR_NUMBER="${{ github.event.pull_request.number }}"
          
          # PRの状態を確認
          PR_INFO=$(gh pr view $PR_NUMBER --repo ${{ github.repository }} --json state,isDraft)
          PR_STATE=$(echo "$PR_INFO" | jq -r '.state')
          IS_DRAFT=$(echo "$PR_INFO" | jq -r '.isDraft')
          
          echo "PR状態: $PR_STATE"
          echo "Draft: $IS_DRAFT"
          
          if [ "$PR_STATE" != "OPEN" ]; then
            echo "PRがオープン状態ではありません"
            exit 1
          fi
          
          # Draft PRの場合は自動的に Ready for review に変更
          if [ "$IS_DRAFT" = "true" ]; then
            echo "📝 Draft PRを検出しました。Ready for review に変更します..."
            gh pr ready $PR_NUMBER --repo ${{ github.repository }}
            echo "✓ PRをReady状態に変更しました"
            
            # Draft解除後、少し待機（GitHub APIの反映待ち）
            sleep 5
          fi
      
      - name: Auto approve and merge PR
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN || github.token }}
        run: |
          PR_NUMBER="${{ github.event.pull_request.number }}"
          
          echo "✅ PRを自動承認します: #$PR_NUMBER"
          
          # Personal Access Tokenで承認（失敗しても続行）
          gh pr review $PR_NUMBER \
            --repo ${{ github.repository }} \
            --approve \
            --body "✅ Copilotによる改善を自動承認しました。FTPアップロードを実行します。" || echo "⚠️ 承認をスキップ（Rulesetでバイパス設定済み）"
          
          echo ""
          echo "🔀 PRを自動マージします: #$PR_NUMBER"
          
          # Rulesetバイパス権限でマージ（Copilot coding agentに権限付与済み）
          if gh pr merge $PR_NUMBER \
            --repo ${{ github.repository }} \
            --squash \
            --delete-branch; then
            echo "✅ 自動マージが完了しました"
            echo "📤 deploy-to-ftp ワークフローがトリガーされます"
          else
            echo "⚠️ 直接マージ失敗、auto-mergeを試行..."
            # auto-mergeで待機マージ（チェック完了後に自動マージ）
            gh pr merge $PR_NUMBER \
              --repo ${{ github.repository }} \
              --auto \
              --squash \
              --delete-branch && echo "✅ Auto-mergeを設定しました（チェック完了後に自動マージされます）"
          fi
